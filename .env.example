# TeamDesk MCP Server v2 - Configuração
# Copie este arquivo para .env e configure os valores

# =============================================================================
# CONFIGURAÇÃO DO TEAMDESK
# =============================================================================

# ID do banco de dados TeamDesk (obrigatório)
TEAMDESK_DATABASE_ID=101885

# Token Master/Admin do TeamDesk (obrigatório)
# Este token é usado para validar as API Keys dos usuários na tabela API-Keys
# Deve ter permissão de leitura/escrita na tabela API-Keys
TEAMDESK_MASTER_TOKEN=seu_token_master_aqui

# Nome da tabela de API Keys (padrão: API-Keys)
# Esta tabela deve conter os campos: Key, Token, Ativo, Nome, Ultimo_Uso
TEAMDESK_API_KEYS_TABLE=API-Keys

# =============================================================================
# CONFIGURAÇÃO DO SERVIDOR MCP
# =============================================================================

# Host e porta do servidor
MCP_HOST=0.0.0.0
MCP_PORT=8080

# =============================================================================
# SEGURANÇA
# =============================================================================

# Rate limiting: número máximo de requisições por minuto por IP
MCP_RATE_LIMIT=100

# Tamanho máximo de payload em bytes (padrão: 1MB)
MCP_MAX_PAYLOAD_SIZE=1048576

# Origens CORS permitidas (separadas por vírgula, * para todas)
MCP_CORS_ORIGINS=*

# =============================================================================
# PERFORMANCE
# =============================================================================

# TTL do cache de dados em segundos (para list_tables e describe_table)
MCP_CACHE_TTL=300

# TTL do cache de validação de API Keys em segundos
# Após validar uma API Key, ela fica em cache por este tempo
MCP_API_KEY_CACHE_TTL=60
